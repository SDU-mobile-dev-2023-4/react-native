/**
 * White outline button style, used for large secondary buttons
 */
export const OutlineWhiteButtonStyle = {
    backgroundColor: 'transparent',
    alignSelf: 'center',
    borderColor: '#fff',
    borderWidth: 2,
    borderRadius: 15,
    paddingVertical: 10,
    paddingHorizontal: 10,
    width: '80%',
    height: 'auto',
}

/**
 * Outline button style, only containing border styling
 */
export const OutlineButtonStyle = {
    backgroundColor: 'transparent',
    alignSelf: 'center',
    borderColor: '#fff',
    borderWidth: 2,
    borderRadius: 15,
}

/**
 * Full page button style, used for large primary buttons in the color theme
 */
export const RedButtonStyle = {
    backgroundColor: '#EA4646',
    borderRadius: 20,
    paddingVertical: 10,
    paddingHorizontal: 80,
}